server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/orders
    driver-class-name: org.postgresql.Driver
    username: orders
    password: orders
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
